interface:
  display_name: ClearJev
  short_description: Control Jev routing, models, reasoning, and API access (Codex CLI only).
  default_prompt: Show ClearJev status and available actions.
policy:
  # Implicit invocation is limited to routing-control intents (on/off,
  # status, key, models, run). Coding tasks never trigger this skill.
  allow_implicit_invocation: true
