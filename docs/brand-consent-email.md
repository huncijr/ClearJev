# Brand consent request — send to support@typesafe.ai

> Copy-paste template. Subject: `Brand use consent — "ClearJev" open-source Codex plugin using the Jev API`
> Note: this is not a legal filing, just a polite permission request for
> factual use of the name. If they ask, we commit to renaming (fallback
> names: `ClearRouter`, `CodexRoute`).

---

Subject: Brand use consent — "ClearJev" open-source Codex plugin using the Jev API

Hi TypeSafe team,

I'm building **ClearJev** (https://github.com/huncijr/ClearJev), a free,
open-source routing layer for Codex: a pre-prompt hook classifies each
developer request via the TypeSafe Jev API (`jev-latest`) and injects a short
model/reasoning/planning decision as developer context. Code owns the
workflow; Jev supplies typed Choice/Score/Noul judgments.

Key facts:

- Every user brings their **own API key** — the project does not resell,
  proxy, or share access to your Services.
- No benchmarks are published, no model output is used for training, and no
  competing decision model is being built.
- The project page and plugin metadata state that ClearJev is independent
  and **not affiliated with or endorsed by TypeSafe AI, Inc.**

Per MCA §16.4 (Publicity), I'm writing to ask for consent to:

1. keep the project name **"ClearJev"** ("Jev" referring solely to the
   technical dependency on your API), and
2. describe the dependency factually as **"uses the TypeSafe Jev API
   (bring your own key)"**.

If you'd rather we rename, just say the word — we'll do it promptly
(candidates: `ClearRouter`, `CodexRoute`).

Thanks for Jev and for the docs/cookbooks — the confidence-gating and
composite-scoring patterns shaped this design.

Best regards,
[Your name]
[GitHub: huncijr]
