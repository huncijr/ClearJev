---
description: Remove a model from ClearJev routing
argument-hint: MODEL=<slug>
---

Use `$clearjev` to remove MODEL=$MODEL from routing. If MODEL is missing, list
configured models and ask which one to remove.
