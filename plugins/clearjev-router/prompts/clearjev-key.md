---
description: Configure or inspect the TypeSafe API key
argument-hint: ACTION=status|set|unset [KEY=<key>]
---

Use `$clearjev` to perform API-key ACTION=$ACTION. Warn that keys typed into chat
remain in chat history and recommend terminal setup. If I explicitly provide
KEY=$KEY and ask to continue, save it without echoing it.
