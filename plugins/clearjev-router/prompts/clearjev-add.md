---
description: Add a model and its allowed reasoning levels
argument-hint: MODEL=<slug> [REASONING=all|low,medium,high]
---

Use `$clearjev` to add MODEL=$MODEL with REASONING=$REASONING. If either value
is missing, ask for it. Validate the model against `clearjev models available`.
